# Server

This workspace is reserved for backend/server code for the React Gym AI Planner monorepo.

Run `npm install` from the repository root, then use `npm run start:server` or `npm --workspace Server run start`.
